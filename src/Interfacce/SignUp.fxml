<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>

<AnchorPane prefHeight="400" prefWidth="600" stylesheets="@SignUp.css" xmlns="http://javafx.com/javafx/17.0.2-ea" xmlns:fx="http://javafx.com/fxml/1" fx:controller="Controller.SignUpController">
   <children>
      <HBox prefHeight="400" prefWidth="600">
         <!-- Pannello laterale blu -->
         <Pane prefHeight="451.0" prefWidth="230" styleClass="blue-side-panel">
            <children>
               <ImageView fitHeight="352.0" fitWidth="371.0" layoutX="-81.0" layoutY="50.0" pickOnBounds="true" preserveRatio="true">
                  <image>
                     <Image url="@../Immagini/Logo.png" />
                  </image>
               </ImageView>
            </children></Pane>

         <!-- Contenitore centrale -->
         <StackPane alignment="CENTER" prefHeight="400" prefWidth="450">
            <VBox alignment="CENTER" prefHeight="340" prefWidth="400" spacing="20" styleClass="signin-container">
               <Label styleClass="signin-title" text="SIGN UP" />

               <VBox alignment="CENTER_LEFT" spacing="10">
                  <Label styleClass="signin-label" text="Username" />
                  <TextField fx:id="Username_SignUp" promptText="Username" styleClass="signin-input" />
               </VBox>

               <VBox alignment="CENTER_LEFT" spacing="10">
                  <Label styleClass="signin-label" text="Password" />
                  <PasswordField fx:id="Passw_SignUp" promptText="Password" styleClass="signin-input" />
               </VBox>

               <VBox alignment="CENTER_LEFT" spacing="10">
                  <Label styleClass="signin-label" text="Conferma Password" />
                  <PasswordField fx:id="ConfirmPassw_SignUp" promptText="Conferma Password" styleClass="signin-input" />
               </VBox>

               <HBox alignment="CENTER" spacing="20">
                  <Button fx:id="BackToLogIn" styleClass="signin-back-button" text="Torna al logIn" />
                  <Button fx:id="Button_SignUp" styleClass="signin-button" text="Sign Up" />
               </HBox>
            </VBox>
         </StackPane>
      </HBox>
   </children>
</AnchorPane>
